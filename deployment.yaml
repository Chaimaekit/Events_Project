version: 1
deployments:
  - name: scraper
    entrypoint: workflow.py:print_events
    work_pool:
      name: default
    schedules:
      - cron: "*/2 * * * *"   # every 2 minutes
        timezone: UTC
